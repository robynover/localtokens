<nav class="topnav">
  <div class="navtitle"><h1><a href="/">Local Tokens</a></h1></div>

  <div class="navbuttons">
    <div class="user-id">
      {{#if is_admin}}
        <div class="topnav-admin">
        <a href="/admin">
          <i class="fa fa-cog" aria-hidden="true"></i> 
          Admin</a>
        </div>
      {{/if}}
      {{#if username}}
        <a href="/user">
          <i class="fa fa-user-circle-o"></i>
          {{username}}
        </a>
      {{/if}}
      {{#if loggedin}}
      <div class="alertbell">
        <i class="fa fa-bell" aria-hidden="true"></i>
        <span class="button-badge">&nbsp;</span> 
        <div class="dropdown arrow_box"></div>
      </div> 
      {{/if}}
      
    </div>


    {{!-- {{#if loggedin}}
    {{else}}
      <a href="signup"><button>Sign Up</button></a>
    {{/if}} --}}

    &nbsp;&nbsp;

   <a href="/{{#if loggedin}}signout{{else}}signin{{/if}}"><button>
      {{#if loggedin}}Sign Out{{else}}Sign In{{/if}}
    </button></a>

  </div>

</nav>
<div style="clear:both;"></div>